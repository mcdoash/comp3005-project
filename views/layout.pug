include mixins
html(lang="en")
    head
        title #{title}
        block css
            +css("main")

    body
        nav
            ul
                li
                    a(href="/books") Browse
                li
                    a(href="/cart") Cart
                if !session.signedIn
                    li
                        a(href="/login") Log In/Register
                if session.signedIn
                    li 
                        a(href="/logout") Log Out
                li 
                    a(href="/orders") Orders
                li#owner
                    a(href="/owner") Owner options
                    
        #content
            block content
    
    block scripts